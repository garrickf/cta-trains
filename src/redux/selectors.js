// Query selectors

export const getCurrentStationName = (store) =>
    store.query.currentStation;

export const getCurrentStationId = (store) =>
    store.query.currentStationId;

export const getSearch = (store) =>
    store.query.search;

export const getStations = (store) =>
    store.query.allStations;
